<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="top"
    android:padding="16dp"
    android:background="@drawable/feature_card_background">

    <ImageView
        android:id="@+id/featureIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        app:tint="@color/primary"
        tools:src="@drawable/ic_shield"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/featureTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary_dark"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Browse with Peace of Mind" />

        <TextView
            android:id="@+id/featureDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_secondary_dark"
            android:textSize="14sp"
            android:lineSpacingExtra="2dp"
            tools:text="Secure your connection on public Wi-Fi and protect your personal data." />

    </LinearLayout>

</LinearLayout>
